# Netlify 部署配置
[build]
  command = "npm run build"
  publish = "dist"

# 单页应用路由重定向
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 环境变量（在 Netlify Dashboard → Site settings → Environment variables 中设置）
# VITE_SUPABASE_URL=https://xxx.supabase.co
# VITE_SUPABASE_ANON_KEY=xxx